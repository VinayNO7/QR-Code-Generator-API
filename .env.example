# Generate a long random value before deploying. Never use the development default.
QR_API_SECRET=replace-with-a-long-random-secret
QR_DATABASE_PATH=./data/qr_api.db
QR_TOKEN_TTL_MINUTES=1440
# Comma-separated origins, or * for a public browser client.
QR_CORS_ORIGINS=
